import React from 'react';


const initialState={
    testKey:true
};
export const reducer = (state = initialState, action) => {
    switch(action.type){
        default:
                return state;
    }
};